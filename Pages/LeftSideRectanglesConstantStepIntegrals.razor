@page "/integrals/left-side-rectangles/constant-step"

<ConstantStepAlgorithmLayout 
	BackPage="integrals/left-side-rectangles"
	NumericMethod="Calculate"/>

@code {
	private double Calculate(Func<double, double> function, double a, double b, long n)
	{
		double h = (b - a) / n;
		double x = a;
		double sum = 0.0;
		do {
			sum += function(x);
			x += h;
		} while (x <= b - h);

		return sum * h;
	}
}